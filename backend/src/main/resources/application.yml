server:
  port: 8080

spring:
  application:
    name: blog
  datasource:
    url: jdbc:postgresql://localhost:5432/blogdb
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 1
  jpa:
    hibernate:
      ddl-auto: update
